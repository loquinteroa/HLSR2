import styled from 'styled-components';

const GridBreakComponent = styled.div `width: 100%`;

export const GridBreak: React.FC = () => {

    return (
        <GridBreakComponent className='gridBreak' />
    );
}